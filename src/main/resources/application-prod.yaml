spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_server?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: '@LY20041206'

  jpa:
    database: MYSQL
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update  # ✅ 自动建表/更新表结构
    show-sql: true
    properties:
      hibernate:
        format_sql: true
